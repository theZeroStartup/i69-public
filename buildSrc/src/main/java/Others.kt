object Others {
    private const val glideVersion = "4.16.0"
    private const val retrofitVersion = "2.9.0"
    private const val loggingInterceptorVersion = "4.12.0"
    private const val timberVersion = "5.0.1"
    private const val lottieVersion = "6.2.0"
    private const val oneSignalVersion = "4.6.0"
    private const val expandableLayoutVersion = "2.9.2"
//    private const val chatKitVersion = "0.4.1"
    private const val chatKitVersion = "1.2.0"
    private const val pixImagePickerVersion = "1.6.3"
//    private const val carouselVersion = "0.1.5"
    private const val carouselVersion = "d46b3c394f"
    private const val permissionsVersion = "3.8"
    private const val runtimeWorkerVersion = "2.7.1"

    private const val bom = "32.7.0"
    private const val firebasecrashlytics = "18.2.9"
    private const val firebaseanalytics = "21.5.0"
    private const val firebasemessaging = "23.4.0"

    //    private const val exoversion = "2.14.0"
    private const val exoversion = "2.19.1"
    private const val paypalVersion = "0.8.7"
    private const val googleMapVersion = "18.2.0"

    private const val camera2Version = "1.3.1"

    //ExoPlayer
    const val exoplayer = "com.google.android.exoplayer:exoplayer:$exoversion"

    // Glider
    const val glide = "com.github.bumptech.glide:glide:$glideVersion"
    const val glideCompiler = "com.github.bumptech.glide:compiler:$glideVersion"
    const val glideAnnotation = "com.github.bumptech.glide:annotations:$glideVersion"
    const val glideHttp = "com.github.bumptech.glide:okhttp3-integration:$glideVersion"

    // Retrofit
    const val retrofit = "com.squareup.retrofit2:retrofit:$retrofitVersion"
    const val gsonConverter = "com.squareup.retrofit2:converter-gson:$retrofitVersion"
    const val scalarsCibverter = "com.squareup.retrofit2:converter-scalars:$retrofitVersion"
    const val loggingInterceptor =
        "com.squareup.okhttp3:logging-interceptor:$loggingInterceptorVersion"

    // Timber
    const val timber = "com.jakewharton.timber:timber:$timberVersion"

    // Lottie
    const val lottie = "com.airbnb.android:lottie:$lottieVersion"

    // Expandable Layout
    const val expandableLayout = "com.github.cachapa:ExpandableLayout:$expandableLayoutVersion"

    // Chat UI
//    const val chatKit = "com.github.stfalcon-studio:Chatkit:$chatKitVersion"
     const val pixImagePicker = "io.ak1.pix:piximagepicker:$pixImagePickerVersion"
//    const val carousel = "com.synnapps:carouselview:$carouselVersion"
    const val carousel = "com.github.sayyam:carouselview:$carouselVersion"
//    const val permissions = "com.nabinbhandari.android:permissions:$permissionsVersion"

    // One Signal
    const val oneSignal = "com.onesignal:OneSignal:$oneSignalVersion"

    const val runtimeWorker = "androidx.work:work-runtime-ktx:$runtimeWorkerVersion"

    // Paypal SDK
    const val paypal = "com.paypal.checkout:android-sdk:$paypalVersion"

    //Google Maps
    const val googleMaps = "com.google.android.gms:play-services-maps:$googleMapVersion"


    const val camera2 = "androidx.camera:camera-camera2:$camera2Version"
    const val cameraLifeCycle = "androidx.camera:camera-lifecycle:$camera2Version"
    const val cameraView = "androidx.camera:camera-view:$camera2Version"
    const val cameraCore = "androidx.camera:camera-core:${camera2Version}"
    const val cameraVideo = "androidx.camera:camera-video:${camera2Version}"
    const val cameraExtensions = "androidx.camera:camera-extensions:${camera2Version}"

}